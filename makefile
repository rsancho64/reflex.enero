DEMODIR="myRapp"

buildChat:

buildDemo:

	# mkdir $(DEMODIR)
	# cd $(DEMODIR)
	# python3 -m venv .venv 
	# . .venv/bin/activate
	# pip install reflex --use-deprecated=legacy-resolver

clean:
	rm -Rf $(DEMODIR)




